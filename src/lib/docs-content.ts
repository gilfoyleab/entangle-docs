export interface DocPage {
  slug: string;
  title: string;
  description: string;
  content: string;
}

export interface DocSection {
  title: string;
  pages: DocPage[];
}

export const docsStructure: DocSection[] = [
  {
    title: "OVERVIEW",
    pages: [
    {
      slug: "introduction",
      title: "01  Introduction",
      description: "Introduction documentation",
      content: "<h1 id=\"introduction\">Introduction</h1>\n<p>Entangle Protocol is a <strong>Bittensor subnet</strong> that provides fully decentralised cross-chain message relaying across 13 blockchain networks and 5 distinct ecosystems. It connects EVM chains (Ethereum, Arbitrum, Optimism, Base, Polygon, BNB Chain, Avalanche), Solana, SUI, Stellar, and Cosmos chains (Cosmos Hub, Osmosis, Neutron) using a competitive, incentivised relay network.</p>\n<p><strong>No bridges. No custodians. No single point of failure.</strong> Just a decentralised network of incentivised agentic relayers — scored and rewarded by Bittensor&#39;s Yuma Consensus.</p>\n<h3 id=\"what-makes-entangle-different\">What makes Entangle different</h3>\n<table>\n<thead>\n<tr>\n<th>Dimension</th>\n<th>Entangle</th>\n<th>Wormhole</th>\n<th>LayerZero</th>\n<th>Axelar</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Miner entry</strong></td>\n<td>Permissionless (TAO burn)</td>\n<td>Permissioned Guardians</td>\n<td>Permissioned DVNs</td>\n<td>Permissioned validators</td>\n</tr>\n<tr>\n<td><strong>Incentive model</strong></td>\n<td>Competitive TAO emissions + fees</td>\n<td>Protocol revenue share</td>\n<td>DVN operator fees</td>\n<td>Validator staking rewards</td>\n</tr>\n<tr>\n<td><strong>Source-chain watching</strong></td>\n<td>Specialised scanner miners</td>\n<td>Each Guardian node</td>\n<td>Each DVN</td>\n<td>Each validator</td>\n</tr>\n<tr>\n<td><strong>Decentralisation</strong></td>\n<td>Open subnet, any operator</td>\n<td>19 Guardian multisig</td>\n<td>DVN operator set</td>\n<td>Proof-of-stake set</td>\n</tr>\n<tr>\n<td><strong>Latency target</strong></td>\n<td>&lt;60s fast chains / &lt;90s EVM L1</td>\n<td>1–5 min</td>\n<td>&lt;1 min</td>\n<td>2–5 min</td>\n</tr>\n<tr>\n<td><strong>Fee model</strong></td>\n<td>Oracle-driven per-destination</td>\n<td>Fixed/variable</td>\n<td>Pay-as-you-go</td>\n<td>Fixed</td>\n</tr>\n<tr>\n<td><strong>Chain extensibility</strong></td>\n<td>ChainAdapter interface</td>\n<td>Per-chain deployments</td>\n<td>DVN per chain</td>\n<td>Per-chain voting</td>\n</tr>\n</tbody></table>\n<h3 id=\"protocol-at-a-glance\">Protocol at a Glance</h3>\n<pre><code>Protocol Version:   2.0\nChains Supported:   13\nEcosystems:         5 (EVM, Solana, SUI, Cosmos, Stellar)\nMiner Types:        2 (Scanner &amp; Relay)\nEmission Split:     30% Discovery / 70% Relay\nGovernance:         3-of-5 Multisig with 48h timelocks\nLatency Target:     &lt;60s (fast chains), &lt;90s (EVM L1)\n</code></pre>\n<hr>\n"
    },
    {
      slug: "why-entangle-exists",
      title: "02  Why Entangle Exists",
      description: "Why Entangle Exists documentation",
      content: "<h1 id=\"why-entangle-exists\">Why Entangle Exists</h1>\n<h3 id=\"the-web3-stack-is-fragmented\">The Web3 Stack is Fragmented</h3>\n<p>Billions of dollars have been stolen from bridges. Trillions in value sits siloed across chains that cannot communicate. Cross-chain infrastructure is the most critical unsolved problem in Web3.</p>\n<p><strong>Isolated Networks</strong><br>Ethereum cannot talk to Solana. Solana cannot talk to Cosmos. Each blockchain is an island — its assets and logic locked behind an invisible wall, unable to compose with the broader ecosystem.</p>\n<blockquote>\n<p>$2.8B lost to bridge exploits in 2022–2024</p>\n</blockquote>\n<p><strong>Fragile Bridges</strong><br>Trusted bridges require handing custody to a multisig. Lock-and-mint mechanisms create honeypots. Validator collusion is a single exploit away from draining everything. The model is architecturally broken.</p>\n<blockquote>\n<p>67% of crypto hacks target bridge contracts</p>\n</blockquote>\n<p><strong>Centralised Relayers</strong><br>Most cross-chain messaging protocols rely on permissioned relayer sets that require KYC, stake in specific tokens, or DAO membership. This is not decentralisation — it is a distributed trust assumption with extra steps.</p>\n<blockquote>\n<p>Top 3 relayer protocols control 78% of cross-chain volume</p>\n</blockquote>\n<h3 id=\"the-entangle-solution\">The Entangle Solution</h3>\n<p>TCP/IP didn&#39;t ask permission to connect computers. It established a protocol — and let the network do the rest. Entangle does the same for blockchains.</p>\n<p>Instead of a bridge, Entangle is a network. Instead of a trusted relayer, Entangle uses decentralised miners — scored by Bittensor&#39;s Yuma Consensus — competing in an open market to route messages faster, cheaper, and more reliably than any centralised alternative could.</p>\n<p>Cross-chain communication becomes a decentralised digital commodity. <strong>Anyone can mine it. Anyone can validate it. Anyone can build on it.</strong></p>\n<hr>\n"
    },
    {
      slug: "system-overview",
      title: "03  System Overview",
      description: "System Overview documentation",
      content: "<h1 id=\"system-overview\">System Overview</h1>\n<p>Every cross-chain message passes through the same three-phase lifecycle:</p>\n<pre><code>┌─────────────────────────────────────────────────────────────────────┐\n│                     ENTANGLE MESSAGE LIFECYCLE                       │\n├──────────────┬────────────────────────────┬────────────────────────┤\n│   PHASE 1    │         PHASE 2            │        PHASE 3         │\n│    SCAN      │          AUCTION           │         RELAY          │\n│              │                            │                        │\n│  Scanner     │  Validators verify events, │  Winning relay miner   │\n│  miners      │  collect threshold         │  executes delivery on  │\n│  watch source│  attestations, and run     │  destination chain     │\n│  chains for  │  sealed-bid relay auction: │  using validator-issued │\n│  events      │  selecting winner by       │  threshold signature   │\n│              │  latency, gas, history     │  bundle                │\n│              │                            │                        │\n│  ← 30% TAO → │       Orchestration        │   ← 70% TAO ←         │\n└──────────────┴────────────────────────────┴────────────────────────┘\n</code></pre>\n<h3 id=\"the-message-lifecycle-step-by-step\">The Message Lifecycle — Step by Step</h3>\n<p><strong>Step 1 — dApp Sends</strong><br>A dApp calls <code>sendMessage()</code> on the Entangle contract. Fee is split into a platform fee and relay reserve.</p>\n<p><strong>Step 2 — Scanners Detect</strong><br>Scanner miners race to detect the <code>MessageDispatched</code> event via private RPC. The first verified response earns the highest score.</p>\n<p><strong>Step 3 — Validators Sign</strong><br>Validators spot-verify events, serialise canonical message fields, and produce a threshold signature bundle using chain-specific attestation keys.</p>\n<p><strong>Step 4 — Relay Delivers</strong><br>The winning relay miner from the sealed-bid auction calls <code>receiveEntangleMessage()</code> on the destination dApp.</p>\n<h3 id=\"network-topology\">Network Topology</h3>\n<pre><code>                   BITTENSOR METAGRAPH\n                   (subnet registration,\n                    UID assignments,\n                    weight aggregation)\n                          │\n           ┌──────────────┼──────────────┐\n           │              │              │\n      ┌────┴────┐    ┌────┴────┐    ┌────┴────┐\n      │  VAL-1  │    │  VAL-2  │    │  VAL-N  │\n      └────┬────┘    └────┬────┘    └────┬────┘\n           │              │              │\n           └──────┬────────┘──────┬──────┘\n                  │               │\n        ┌─────────┴──┐     ┌──────┴──────────┐\n        │ SCANNER     │     │  RELAY          │\n        │ MINERS      │     │  MINERS         │\n        │ (M1 pool)   │     │  (M2 pool)      │\n        └──────┬──────┘     └──────┬──────────┘\n               │                   │\n   ┌───────────┴──────┐    ┌───────┴──────────────┐\n   │  SOURCE CHAINS   │    │  DESTINATION CHAINS  │\n   │ • ETH mainnet    │    │ • Arbitrum           │\n   │ • Solana         │    │ • Optimism           │\n   │ • Cosmos Hub     │    │ • SUI                │\n   │ • SUI            │    │ • Stellar            │\n   │ • Stellar        │    │ • ETH mainnet        │\n   └──────────────────┘    └──────────────────────┘\n        (Entangle contracts deployed on all chains)\n</code></pre>\n<hr>\n"
    }
    ]
  },
  {
    title: "ARCHITECTURE",
    pages: [
    {
      slug: "architecture-deep-dive",
      title: "04  Architecture Deep Dive",
      description: "Architecture Deep Dive documentation",
      content: "<h1 id=\"architecture-deep-dive\">Architecture Deep Dive</h1>\n<h3 id=\"41-key-design-principles\">4.1 Key Design Principles</h3>\n<p><strong>Permissionless Entry</strong><br>Any operator can register by paying the dynamic TAO burn. No approval process, no guardian whitelist, no DVN gatekeeping. Competition is open.</p>\n<p><strong>Objective Scoring</strong><br>Every score dimension uses on-chain or cryptographically verifiable data. Validator receipt timestamps prevent self-reporting manipulation entirely.</p>\n<p><strong>Sealed-Bid Auctions</strong><br>Relay miners compete on latency and gas estimates before execution. Bid accuracy is scored. The market finds optimal pricing automatically.</p>\n<p><strong>ChainAdapter Interface</strong><br>New chains can be added without deploying new contracts. Same-ecosystem chains are zero-config additions. New ecosystems need one Python class implementing five methods.</p>\n<p><strong>One Contract Per Chain</strong><br>A single Entangle contract handles both source (<code>sendMessage</code>) and destination (<code>verifyMessage</code>) roles on every supported blockchain.</p>\n<p><strong>3-of-5 Multisig Governance</strong><br>All protocol governance through a 3-of-5 Gnosis Safe. 48h timelocks on validator set and threshold changes. All transactions publicly verifiable on-chain.</p>\n<h3 id=\"42-protocol-data-structures\">4.2 Protocol Data Structures</h3>\n<h4>MessageEnvelope</h4>\n<p>The chain-agnostic, canonical description of a pending cross-chain message:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>message_id</code></td>\n<td>sha256 of (src_chain + protocol_addr + seq + src_tx). Globally unique.</td>\n</tr>\n<tr>\n<td><code>src_chain</code></td>\n<td>Source chain name (e.g. <code>&quot;ethereum&quot;</code>, <code>&quot;solana&quot;</code>)</td>\n</tr>\n<tr>\n<td><code>src_ecosystem</code></td>\n<td>Ecosystem family: <code>evm</code>, <code>solana</code>, <code>sui</code>, <code>stellar</code>, <code>cosmos</code></td>\n</tr>\n<tr>\n<td><code>src_protocol</code></td>\n<td>Entangle ProtocolHub contract address on the source chain</td>\n</tr>\n<tr>\n<td><code>src_tx</code></td>\n<td>Transaction hash / signature of the <code>MessageDispatched</code> event</td>\n</tr>\n<tr>\n<td><code>src_seq</code></td>\n<td>Monotonically increasing sequence number from the source hub</td>\n</tr>\n<tr>\n<td><code>src_height</code></td>\n<td>Block / slot / ledger number on the source chain</td>\n</tr>\n<tr>\n<td><code>src_timestamp</code></td>\n<td>Unix timestamp of source transaction confirmation</td>\n</tr>\n<tr>\n<td><code>dst_chain</code></td>\n<td>Target chain name</td>\n</tr>\n<tr>\n<td><code>dst_ecosystem</code></td>\n<td>Target ecosystem family</td>\n</tr>\n<tr>\n<td><code>dst_protocol</code></td>\n<td>Entangle ProtocolHub address on destination chain</td>\n</tr>\n<tr>\n<td><code>dst_recipient</code></td>\n<td>Application contract/account receiving the message</td>\n</tr>\n<tr>\n<td><code>payload_hex</code></td>\n<td>Hex-encoded raw message payload (max 8 KiB)</td>\n</tr>\n<tr>\n<td><code>requires_ack</code></td>\n<td>If true, source dApp expects an acknowledgement reply</td>\n</tr>\n</tbody></table>\n<h4>ProofBundle</h4>\n<p>Returned by the miner as evidence of delivery:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>proof_type</code></td>\n<td>One of: <code>evm_tx_receipt</code>, <code>solana_signature</code>, <code>sui_digest</code>, <code>stellar_hash</code>, <code>cosmos_tx_hash</code></td>\n</tr>\n<tr>\n<td><code>relay_tx</code></td>\n<td>Primary on-chain identifier of the relay transaction</td>\n</tr>\n<tr>\n<td><code>dst_height</code></td>\n<td>Block / slot / ledger number on the destination chain</td>\n</tr>\n<tr>\n<td><code>dst_timestamp</code></td>\n<td>Unix timestamp of the relay confirmation</td>\n</tr>\n<tr>\n<td><code>raw</code></td>\n<td>Ecosystem-specific data (EVM receipt/logs, Solana signature/slot, SUI digest/effects, etc.)</td>\n</tr>\n</tbody></table>\n<h4>RelayStatus Codes</h4>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Name</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>200</td>\n<td><code>SUCCESS</code></td>\n<td>Relay confirmed on destination chain</td>\n</tr>\n<tr>\n<td>208</td>\n<td><code>ALREADY_RELAYED</code></td>\n<td>Another miner relayed first. Partial score.</td>\n</tr>\n<tr>\n<td>202</td>\n<td><code>PENDING</code></td>\n<td>Transaction submitted but not yet confirmed</td>\n</tr>\n<tr>\n<td>402</td>\n<td><code>INSUFFICIENT_FUNDS</code></td>\n<td>Miner wallet has insufficient gas</td>\n</tr>\n<tr>\n<td>500</td>\n<td><code>REVERT</code></td>\n<td>Destination contract reverted</td>\n</tr>\n<tr>\n<td>408</td>\n<td><code>TIMEOUT</code></td>\n<td>Message not confirmed before deadline</td>\n</tr>\n<tr>\n<td>501</td>\n<td><code>UNSUPPORTED_CHAIN</code></td>\n<td>Miner doesn&#39;t support this chain pair</td>\n</tr>\n<tr>\n<td>422</td>\n<td><code>INVALID_PROOF</code></td>\n<td>Miner couldn&#39;t build a valid proof</td>\n</tr>\n<tr>\n<td>429</td>\n<td><code>RATE_LIMITED</code></td>\n<td>Miner is at capacity — task shed</td>\n</tr>\n</tbody></table>\n<h3 id=\"43-deterministic-task-ids\">4.3 Deterministic Task IDs</h3>\n<p>Task IDs are computed deterministically so all validators assign the same ID to the same message — critical for cross-validator score consistency:</p>\n<pre><code class=\"language-python\">task_id = sha256(\n    sha256(src_chain + &quot;:&quot; + protocol_addr + &quot;:&quot; + seq + &quot;:&quot; + src_tx)\n    + &quot;:&quot; + validator_hotkey[:8]\n)\n</code></pre>\n<h3 id=\"44-deployment-architecture\">4.4 Deployment Architecture</h3>\n<p><strong>Per Validator Node:</strong></p>\n<pre><code>┌────────────────────────────────────────────────────────────┐\n│  validator-process (Python)                                │\n│    ├─ BittensorClient (metagraph, axon, dendrite)          │\n│    ├─ ChainAdapterRegistry (one adapter per chain)         │\n│    ├─ SynapseHandlers (ChainScan, HealthCheck, Attest)     │\n│    ├─ AuctionEngine (BidRequest, winner selection)         │\n│    ├─ ExecutionMonitor (proof verification, scoring)       │\n│    ├─ OracleSubmitter (gas price updates)                  │\n│    └─ WeightCommitter (set_weights every epoch)            │\n│                                                            │\n│  HSM / Secrets Manager (external)                         │\n│    └─ Attestation keys per dst_chain ecosystem            │\n└────────────────────────────────────────────────────────────┘\n</code></pre>\n<p><strong>Per Scanner Miner Node:</strong></p>\n<pre><code>┌────────────────────────────────────────────────────────────┐\n│  scanner-process (Python)                                  │\n│    ├─ BittensorClient (axon — serves ChainScanSynapse)     │\n│    ├─ ChainAdapterRegistry (source chain adapters only)    │\n│    └─ SynapseHandlers (ChainScan, HealthCheck)             │\n│                                                            │\n│  RPC Infrastructure:                                       │\n│    ├─ Private dedicated RPC (primary) — per chain         │\n│    └─ Public backup RPC — per chain                       │\n└────────────────────────────────────────────────────────────┘\n</code></pre>\n<p><strong>Per Relay Miner Node:</strong></p>\n<pre><code>┌────────────────────────────────────────────────────────────┐\n│  relay-process (Python)                                    │\n│    ├─ BittensorClient (axon — serves BidRequest, Execute)  │\n│    ├─ ChainAdapterRegistry (destination chain adapters)    │\n│    ├─ WalletManager (keys + balances per dst_chain)        │\n│    └─ SynapseHandlers (BidRequest, Execute, Standby)       │\n│                                                            │\n│  Hot Wallets:                                              │\n│    └─ Funded address per supported destination chain      │\n└────────────────────────────────────────────────────────────┘\n</code></pre>\n<hr>\n"
    },
    {
      slug: "dual-miner-model",
      title: "05  Dual-Miner Model",
      description: "Dual-Miner Model documentation",
      content: "<h1 id=\"dual-miner-model\">Dual-Miner Model</h1>\n<p>Entangle uses Bittensor&#39;s <strong>dual incentive mechanism</strong> to run two separate scoring systems on a single subnet. Each mechanism has its own task type, miner population, and emission allocation.</p>\n<table>\n<thead>\n<tr>\n<th>Mechanism</th>\n<th>Name</th>\n<th>Emission Share</th>\n<th>Miner Role</th>\n<th>Task</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Mechanism 1</td>\n<td>Discovery</td>\n<td>30%</td>\n<td>Scanner Miners</td>\n<td>Detect <code>MessageDispatched</code> events on source chains</td>\n</tr>\n<tr>\n<td>Mechanism 2</td>\n<td>Relay</td>\n<td>70%</td>\n<td>Relay Miners</td>\n<td>Execute message delivery to destination chains</td>\n</tr>\n</tbody></table>\n<h3 id=\"51-scanner-miners\">5.1 Scanner Miners</h3>\n<p>Scanner miners are the event detection layer. They monitor source chain contracts continuously for <code>MessageDispatched</code> events and report them to validators.</p>\n<p><strong>How scoring works:</strong></p>\n<p>When a validator broadcasts a <code>ChainScanSynapse</code>, scanner miners race to respond with the event data. The validator records its own receipt timestamp — miners never self-report timing. Rank-based scoring is applied:</p>\n<table>\n<thead>\n<tr>\n<th>Detection Rank</th>\n<th>Score</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1st (fastest)</td>\n<td>1.00</td>\n</tr>\n<tr>\n<td>2nd</td>\n<td>0.70</td>\n</tr>\n<tr>\n<td>3rd</td>\n<td>0.50</td>\n</tr>\n<tr>\n<td>4th</td>\n<td>0.20</td>\n</tr>\n<tr>\n<td>Did not respond / wrong data</td>\n<td>0.00</td>\n</tr>\n</tbody></table>\n<p><strong>What scanner miners need:</strong></p>\n<ul>\n<li>Private, low-latency RPC endpoints for each monitored chain (public RPCs create systematic disadvantage)</li>\n<li>Bittensor hotkey + coldkey</li>\n<li>No funded wallets required (read-only operation)</li>\n<li>Low-bandwidth, read-heavy server infrastructure</li>\n</ul>\n<p><strong>Economics:</strong> Scanner miners receive 30% of subnet TAO emissions. Low capital requirements make this role accessible to a broad population — intentionally, since the Discovery mechanism benefits from many independent, geographically diverse monitors.</p>\n<h3 id=\"52-relay-miners\">5.2 Relay Miners</h3>\n<p>Relay miners are the execution layer. They receive verified relay tasks from validators, submit execution transactions on destination chains, and return cryptographic proof of delivery.</p>\n<p><strong>Auction mechanism:</strong></p>\n<p>When validators have a verified <code>MessageDispatched</code> event, they open a sealed-bid auction:</p>\n<pre><code>Validator broadcasts BidRequest\n        │\n        ├── Relay miners submit bids: { estimated_latency_ms, gas_estimate_usd }\n        │\n        ├── Validator selects winner (lowest combined score of latency + gas × weight)\n        │\n        ├── Winner executes relay transaction\n        │\n        └── Validator scores execution and updates Bittensor weights\n</code></pre>\n<p><strong>Relay execution flow:</strong></p>\n<ol>\n<li>Dedup check — if <code>(src_chain, seq_no, dst_chain)</code> already in-flight, return cached result</li>\n<li>Pre-flight checks — verify chain support, relay key, and deadline</li>\n<li>Mark in-flight — insert entry before execution</li>\n<li>Build <code>PendingMessage</code> from relay task</li>\n<li>Execute <code>adapter.relay_message()</code> under concurrency semaphore (default: 12 parallel)</li>\n<li>Map result → <code>RelayStatus</code> code</li>\n<li>Build <code>ProofBundle</code> with relay tx and verification data</li>\n<li>Mark done and cache for future dedup</li>\n</ol>\n<p><strong>Economics:</strong> Relay miners receive 70% of subnet TAO emissions. Higher risk (funded wallets, capital exposure to gas volatility) is compensated by higher rewards. A relay miner consistently winning 10% of relay rounds on a medium-volume network earns significantly more than a top scanner miner.</p>\n<h3 id=\"53-combined-scanner-relay-miners\">5.3 Combined Scanner + Relay Miners</h3>\n<p>A single Bittensor UID can register both roles by declaring <code>roles: [&quot;scanner&quot;, &quot;relay&quot;]</code> in <code>HealthCheckSynapse</code>. This miner participates in both mechanisms and is scored independently on each.</p>\n<p>The key advantage: a combined miner that detects an event via its scanner function can pre-position its relay function before the <code>BidRequest</code> arrives — a meaningful latency advantage in competitive rounds.</p>\n<blockquote>\n<p><strong>Note:</strong> Running both roles on a single node adds operational complexity. Scanner and relay functions have different resource profiles and failure modes. Experienced operators may prefer separate UIDs.</p>\n</blockquote>\n<hr>\n"
    },
    {
      slug: "validators",
      title: "06  Validators",
      description: "Validators documentation",
      content: "<h1 id=\"validators\">Validators</h1>\n<p>Validators are the orchestration layer. They do not run chain-scanning infrastructure themselves — they outsource scanning to scanner miners and verify results.</p>\n<h3 id=\"validator-responsibilities\">Validator Responsibilities</h3>\n<ul>\n<li>Broadcast <code>ChainScanSynapse</code> to scanner miners and record receipt timestamps</li>\n<li>Spot-verify events (~20% sampled; all events via multi-reporter consensus)</li>\n<li>Maintain a consensus gas oracle that drives the per-destination fee model</li>\n<li>Sign verified messages with chain-specific attestation keys (cryptographic bridge between Bittensor&#39;s sr25519 keys and destination chain signature schemes)</li>\n<li>Run sealed-bid auctions and select relay miner winners</li>\n<li>Independently verify relay execution on destination chains</li>\n<li>Call <code>set_weights()</code> on the Bittensor metagraph every epoch (default: every 100 blocks)</li>\n</ul>\n<h3 id=\"validator-loop\">Validator Loop</h3>\n<pre><code>Step 1: Query       ChainScanSynapse → scanner miners\n                    Record all receipt timestamps server-side\nStep 2: Score       Rank miners by validator receipt time\n                    discovery_score ∈ {0.00, 0.20, 0.50, 0.70, 1.00}\nStep 3: Auction     BidRequest → relay miners\n                    winner selection → execute signal\nStep 4: Verify      RelayTask → relay miners\n                    Independent on-chain verification of delivery\nStep 5: EMA         Exponential Moving Average blends task scores into weights\nStep 6: Weights     set_weights() normalises non-zero EMA scores\n                    → submits to Bittensor chain\nBackground:         HealthCheckSynapse every 600s to up to 20 miners\n</code></pre>\n<h3 id=\"attestation-model\">Attestation Model</h3>\n<p>Validators maintain separate attestation keys for each destination chain ecosystem:</p>\n<pre><code class=\"language-bash\">ENTANGLE_ATTEST_KEY_EVM=0xPRIVKEY       # secp256k1 — all EVM chains\nENTANGLE_ATTEST_KEY_SOLANA=BASE58        # ed25519 — Solana\nENTANGLE_ATTEST_KEY_SUI=BASE58           # ed25519 — SUI\nENTANGLE_ATTEST_KEY_COSMOS=0x...         # secp256k1 — Cosmos chains\nENTANGLE_ATTEST_KEY_STELLAR=BASE58       # ed25519 — Stellar\n</code></pre>\n<p>Keys are loaded into memory at startup and never written to disk. Use an HSM or encrypted secrets manager (AWS Secrets Manager, HashiCorp Vault).</p>\n<hr>\n"
    }
    ]
  },
  {
    title: "PROTOCOL",
    pages: [
    {
      slug: "scoring-incentives",
      title: "07  Scoring & Incentives",
      description: "Scoring & Incentives documentation",
      content: "<h1 id=\"scoring-incentives\">Scoring & Incentives</h1>\n<h3 id=\"71-discovery-score-scanner-miners\">7.1 Discovery Score (Scanner Miners)</h3>\n<p>A scanner miner&#39;s weight is derived from per-event discovery scores averaged across the scoring window (one Bittensor epoch; default 100 blocks):</p>\n<pre><code>period_discovery_score(miner) = mean(discovery_score_i for all events i)\n\nwhere events i = all unique (chain_id, seq_no) events in the scoring window\nand discovery_score_i ∈ {0.00, 0.20, 0.50, 0.70, 1.00}\n</code></pre>\n<p>Miners monitoring more chains observe more events and can score on more of them — incentivising broad chain coverage.</p>\n<h3 id=\"72-execution-score-relay-miners\">7.2 Execution Score (Relay Miners)</h3>\n<p>Relay miners are scored across five independent dimensions:</p>\n<table>\n<thead>\n<tr>\n<th>Dimension</th>\n<th>Weight</th>\n<th>What It Measures</th>\n<th>Score Logic</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>SUCCESS</strong></td>\n<td>50%</td>\n<td>Did the relay land on the destination?</td>\n<td>1.0 = confirmed, 0.4 = already relayed, 0.15 = pending, 0.0 = failed</td>\n</tr>\n<tr>\n<td><strong>SPEED</strong></td>\n<td>25%</td>\n<td>How fast relative to peers?</td>\n<td>relay_block_timestamp − source_block_timestamp (from on-chain headers)</td>\n</tr>\n<tr>\n<td><strong>CORRECTNESS</strong></td>\n<td>15%</td>\n<td>Was the proof valid and verifiable?</td>\n<td>Multi-RPC quorum query; asymmetric fallback if validator infra fails</td>\n</tr>\n<tr>\n<td><strong>FEE_EFFICIENCY</strong></td>\n<td>5%</td>\n<td>Was gas cost reasonable vs the oracle?</td>\n<td>Derived from on-chain receipt, NOT miner-reported</td>\n</tr>\n<tr>\n<td><strong>RELIABILITY</strong></td>\n<td>5%</td>\n<td>Historical success rate (50-task rolling window)</td>\n<td>0.50 neutral for new miners → graduated baseline</td>\n</tr>\n</tbody></table>\n<p>The final blended score per relay round:</p>\n<pre><code>winner_score     = 0.80 × execution_score + 0.20 × bid_quality_score\nnon_winner_score = bid_quality_score (for miners that bid but didn&#39;t win)\nno_bid           = 0.00\n</code></pre>\n<h3 id=\"73-new-relay-miner-reliability-baseline\">7.3 New Relay Miner Reliability Baseline</h3>\n<p>New miners don&#39;t have 50 tasks of history. A graduated baseline prevents impossible first-mover economics:</p>\n<table>\n<thead>\n<tr>\n<th>Tasks Completed</th>\n<th>Reliability Score Applied</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>0–5</td>\n<td>0.50 (neutral baseline; no penalty, no bonus)</td>\n</tr>\n<tr>\n<td>6–15</td>\n<td>Rolling average of completed tasks only</td>\n</tr>\n<tr>\n<td>16–49</td>\n<td>Rolling average, floor of 0.20</td>\n</tr>\n<tr>\n<td>50+</td>\n<td>Full 50-task rolling average, no floor</td>\n</tr>\n</tbody></table>\n<h3 id=\"74-emission-split\">7.4 Emission Split</h3>\n<p>The 30/70 Discovery/Relay emission split is a Bittensor subnet hyperparameter controlled by the subnet owner coldkey (multisig). For every 10 TAO emitted to the subnet: 3 TAO → scanner miner rewards, 7 TAO → relay miner rewards (before Bittensor&#39;s standard validator commission).</p>\n<h3 id=\"75-anti-gaming-protections\">7.5 Anti-Gaming Protections</h3>\n<p><strong>Self-reporting manipulation prevented:</strong> Scanner miner speed rankings use the validator&#39;s own receipt timestamp. Miners never submit a timestamp field. The only way to rank higher is to genuinely respond faster.</p>\n<p><strong>Proof fabrication prevented (three-layer correctness check):</strong></p>\n<ul>\n<li>Layer 1 — Attestation consistency (local): Did the miner include the validator&#39;s attestation, unmodified?</li>\n<li>Layer 2 — Destination chain event query (multi-RPC quorum): Did the relay actually land on-chain?</li>\n<li>Layer 3 — Asymmetric fallback: If quorum fails, liveness probe determines whether it&#39;s validator infra failure (→ 0.5 neutral) or miner fabrication (→ 0.0)</li>\n</ul>\n<p><strong>ALREADY_RELAYED farming prevented:</strong> Partial credit for ALREADY_RELAYED (208) requires the miner to provide raw signed transaction bytes as proof of genuine attempt. Without this: 0.0.</p>\n<p><strong>Fee self-reporting eliminated:</strong> <code>fee_paid_usd</code> is derived entirely from on-chain receipts via Chainlink/Pyth price oracles. Miner-reported values are ignored for scoring.</p>\n<hr>\n"
    },
    {
      slug: "smart-contracts",
      title: "08  Smart Contracts",
      description: "Smart Contracts documentation",
      content: "<h1 id=\"smart-contracts\">Smart Contracts</h1>\n<h3 id=\"81-one-contract-per-chain\">8.1 One Contract Per Chain</h3>\n<p>Entangle deploys <strong>one contract per supported blockchain</strong>. A single deployment serves both directions:</p>\n<ul>\n<li><strong>Source-side</strong>: When a dApp calls <code>sendMessage()</code>, the contract collects fees and emits <code>MessageDispatched</code></li>\n<li><strong>Destination-side</strong>: When a relay miner delivers a message, it calls the dApp&#39;s <code>receiveEntangleMessage()</code>, which calls back into the Entangle contract&#39;s <code>verifyMessage()</code> to validate the threshold signature bundle</li>\n</ul>\n<h3 id=\"82-core-contract-functions\">8.2 Core Contract Functions</h3>\n<table>\n<thead>\n<tr>\n<th>Function</th>\n<th>Visibility</th>\n<th>Caller</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>sendMessage(dst_chain, dst_addr, payload)</code></td>\n<td>external payable</td>\n<td>Any dApp</td>\n<td>Accepts fee, validates invariant, emits <code>MessageDispatched</code>, increments <code>seq_no</code></td>\n</tr>\n<tr>\n<td><code>verifyMessage(msg_hash, sig_bundle)</code></td>\n<td>external view</td>\n<td>Destination dApp</td>\n<td>Verifies threshold signature bundle against registered validator set. Returns <code>bool</code>.</td>\n</tr>\n<tr>\n<td><code>updateGasOracle(dst_chain, gas_estimate)</code></td>\n<td>external</td>\n<td>Registered validator</td>\n<td>Submits gas cost estimate; contract computes stake-weighted median</td>\n</tr>\n<tr>\n<td><code>setBaseFee(dst_chain, amount)</code></td>\n<td>external</td>\n<td>Protocol multisig</td>\n<td>Sets platform base fee for a destination chain</td>\n</tr>\n<tr>\n<td><code>setGasBuffer(dst_chain, multiplier)</code></td>\n<td>external</td>\n<td>Protocol multisig</td>\n<td>Sets gas reserve safety multiplier</td>\n</tr>\n<tr>\n<td><code>setFeeSplit(dst_chain, platform_bps)</code></td>\n<td>external</td>\n<td>Protocol multisig</td>\n<td>Sets platform_fee / relay_reserve ratio</td>\n</tr>\n<tr>\n<td><code>updateValidatorSet(validators[], weights[])</code></td>\n<td>external</td>\n<td>Protocol multisig (timelocked)</td>\n<td>Replaces registered validator public keys and stake weights</td>\n</tr>\n<tr>\n<td><code>setSignatureThreshold(threshold)</code></td>\n<td>external</td>\n<td>Protocol multisig (timelocked)</td>\n<td>Sets minimum validator signatures for <code>verifyMessage()</code> to pass</td>\n</tr>\n<tr>\n<td><code>freezeOracle(dst_chain, value)</code></td>\n<td>external</td>\n<td>Protocol multisig</td>\n<td>Emergency freeze of gas oracle at a manual value</td>\n</tr>\n<tr>\n<td><code>withdrawRelayReserve(dst_chain, amount)</code></td>\n<td>external</td>\n<td>Protocol multisig</td>\n<td>Withdraws accumulated relay reserve</td>\n</tr>\n<tr>\n<td><code>getTotalFee(dst_chain)</code></td>\n<td>view</td>\n<td>Anyone</td>\n<td>Returns current total fee — helper for dApp UIs</td>\n</tr>\n<tr>\n<td><code>getSeqNo(src_addr, dst_chain)</code></td>\n<td>view</td>\n<td>Anyone</td>\n<td>Returns next sequence number for a sender/destination pair</td>\n</tr>\n</tbody></table>\n<h3 id=\"83-contract-events\">8.3 Contract Events</h3>\n<pre><code class=\"language-solidity\">MessageDispatched(\n    uint64 indexed seq_no,\n    address indexed src_addr,\n    string dst_chain,\n    string dst_addr,\n    bytes payload\n)\n\nMessageExecuted(\n    uint64 indexed seq_no,\n    address indexed relayer,   // winning miner&#39;s relay address\n    address recipient,\n    bytes32 payload_hash\n)\n\nGasOracleUpdated(string dst_chain, uint256 new_reserve, uint256 validator_count)\nValidatorSetUpdated(address[] validators, uint256[] weights, uint256 threshold)\nFeeSplitUpdated(string dst_chain, uint256 platform_bps)\nRelayReserveWithdrawn(string dst_chain, uint256 amount, address recipient)\n</code></pre>\n<h3 id=\"84-fee-invariant\">8.4 Fee Invariant</h3>\n<p>The contract enforces this invariant at every <code>sendMessage()</code> call:</p>\n<pre><code class=\"language-solidity\">require(msg.value &gt;= platform_base_fee[dst_chain] + gas_reserve[dst_chain])\n// gas_reserve[dst_chain] = oracle_median × gas_buffer_multiplier\n</code></pre>\n<p>If the gas oracle hasn&#39;t updated recently and gas prices spike above the reserve, <code>sendMessage()</code> reverts with <code>InsufficientReserve</code> until the oracle catches up. dApps should call <code>getTotalFee(dst_chain)</code> before presenting a relay UI to users.</p>\n<h3 id=\"85-cross-chain-replay-protection-4-tuple-dedup-key\">8.5 Cross-Chain Replay Protection — 4-Tuple Dedup Key</h3>\n<p>To prevent a message intended for one destination from being executed on another, all dedup keys use a 4-tuple:</p>\n<table>\n<thead>\n<tr>\n<th>Layer</th>\n<th>Key</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>EVM contract</td>\n<td><code>keccak256(abi.encode(src_chain, DST_CHAIN_NAME, seq_no))</code></td>\n</tr>\n<tr>\n<td>Solana PDA</td>\n<td><code>[b&quot;executed&quot;, src_chain_hash[:8], dst_chain_hash[:8], seq_le_bytes]</code></td>\n</tr>\n<tr>\n<td>SUI Move</td>\n<td><code>ExecutionKey { src_chain: String, dst_chain: String, seq_no: u64 }</code></td>\n</tr>\n<tr>\n<td>Cosmos</td>\n<td><code>EXECUTED map key: (src_chain, dst_chain, seq_no)</code></td>\n</tr>\n<tr>\n<td>Stellar</td>\n<td><code>DataKey::Executed wraps (src_chain_hash, dst_chain_hash, seq_no)</code></td>\n</tr>\n</tbody></table>\n<hr>\n"
    },
    {
      slug: "dapp-integration",
      title: "09  dApp Integration",
      description: "dApp Integration documentation",
      content: "<h1 id=\"dapp-integration\">dApp Integration</h1>\n<h3 id=\"91-how-delivery-works\">9.1 How Delivery Works</h3>\n<p>When a relay miner wins an auction for a cross-chain message, it calls <code>receiveEntangleMessage()</code> on the <strong>destination dApp contract</strong> — not the Entangle contract. The dApp owns this function and implements its own application logic inside it.</p>\n<pre><code>Relay miner\n  → dApp.receiveEntangleMessage(payload, seq_no, src_chain, src_addr, sig_bundle)\n\nInside dApp.receiveEntangleMessage():\n  1. Check dedup:  require(!executed[seq_no], &#39;ALREADY_EXECUTED&#39;)\n  2. Build hash:   msg_hash = hash(src_chain, dst_chain, seq_no, src_addr, dst_addr, payload)\n  3. Validate:     require(entangle.verifyMessage(msg_hash, sig_bundle))\n  4. Mark done:    executed[seq_no] = true\n  5. App logic:    _handleMessage(payload)   // your custom logic here\n</code></pre>\n<h3 id=\"92-evm-integration-solidity\">9.2 EVM Integration (Solidity)</h3>\n<p><strong>Step 1: Import the Entangle interface</strong></p>\n<pre><code class=\"language-solidity\">// SPDX-License-Identifier: MIT\ninterface IEntangle {\n    function verifyMessage(bytes32 msgHash, bytes calldata sigBundle)\n        external view returns (bool);\n}\n</code></pre>\n<p><strong>Step 2: Implement your dApp contract</strong></p>\n<pre><code class=\"language-solidity\">contract MyDApp {\n    IEntangle public immutable entangle;\n    mapping(uint64 =&gt; bool) public executed;\n\n    constructor(address _entangle) {\n        entangle = IEntangle(_entangle);\n    }\n\n    // Source: send a message to another chain\n    function sendCrossChainMessage(\n        string calldata dst_chain,\n        address dst_addr,\n        bytes calldata payload\n    ) external payable {\n        uint256 fee = IEntangle(address(entangle)).getTotalFee(dst_chain);\n        require(msg.value &gt;= fee, &quot;INSUFFICIENT_FEE&quot;);\n        IEntangle(address(entangle)).sendMessage{value: fee}(\n            dst_chain, dst_addr, payload\n        );\n    }\n\n    // Destination: receive a message from another chain\n    function receiveEntangleMessage(\n        bytes calldata payload,\n        uint64 seq_no,\n        string calldata src_chain,\n        bytes calldata src_addr,\n        bytes calldata sig_bundle\n    ) external {\n        require(!executed[seq_no], &#39;ALREADY_EXECUTED&#39;);\n\n        bytes32 msg_hash = keccak256(abi.encode(\n            src_chain, block.chainid, seq_no, src_addr,\n            address(this), payload\n        ));\n\n        require(entangle.verifyMessage(msg_hash, sig_bundle), &#39;BAD_SIGS&#39;);\n        executed[seq_no] = true;\n        _handleMessage(payload); // implement your logic here\n    }\n\n    function _handleMessage(bytes calldata payload) internal {\n        // your application logic\n    }\n}\n</code></pre>\n<p><strong>Step 3: Query the current fee before sending</strong></p>\n<p>Always call <code>getTotalFee(dst_chain)</code> before presenting a send UI — fees change with the gas oracle:</p>\n<pre><code class=\"language-javascript\">const fee = await entangle.getTotalFee(&quot;arbitrum&quot;);\nawait myDApp.sendCrossChainMessage(&quot;arbitrum&quot;, recipientAddr, payload, { value: fee });\n</code></pre>\n<h3 id=\"93-integration-summary-by-ecosystem\">9.3 Integration Summary by Ecosystem</h3>\n<table>\n<thead>\n<tr>\n<th>Ecosystem</th>\n<th>Relay miner calls</th>\n<th>dApp calls for validation</th>\n<th>Sig scheme</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>EVM (Ethereum, Arbitrum…)</td>\n<td><code>dApp.receiveEntangleMessage()</code></td>\n<td><code>entangle.verifyMessage()</code></td>\n<td>secp256k1 ECDSA</td>\n</tr>\n<tr>\n<td>Solana</td>\n<td><code>receiveEntangleMessage</code> instruction</td>\n<td>Entangle program <code>verify_message</code> CPI</td>\n<td>ed25519</td>\n</tr>\n<tr>\n<td>SUI</td>\n<td>dApp Move function <code>receiveEntangleMessage</code></td>\n<td>Entangle Move <code>verify_message</code> call</td>\n<td>ed25519</td>\n</tr>\n<tr>\n<td>Cosmos / IBC</td>\n<td>dApp CosmWasm <code>receiveEntangleMessage</code></td>\n<td>Entangle CosmWasm <code>verify_message</code></td>\n<td>secp256k1 ECDSA</td>\n</tr>\n<tr>\n<td>Stellar</td>\n<td>dApp contract <code>receiveEntangleMessage</code></td>\n<td>Entangle contract <code>verify_message</code></td>\n<td>ed25519</td>\n</tr>\n</tbody></table>\n<h3 id=\"94-adding-new-chains\">9.4 Adding New Chains</h3>\n<p><strong>Same-ecosystem chain (e.g., a new EVM L2):</strong> Deploy the Entangle contract and add to the Chain Configuration Registry. No code changes required.</p>\n<p><strong>New ecosystem chain:</strong> Requires a new <code>ChainAdapter</code> implementation, an SDK PR, and a governance vote to add it to the chain registry.</p>\n<hr>\n"
    }
    ]
  },
  {
    title: "OPERATIONS",
    pages: [
    {
      slug: "running-a-miner",
      title: "10  Running a Miner",
      description: "Running a Miner documentation",
      content: "<h1 id=\"running-a-miner\">Running a Miner</h1>\n<h3 id=\"101-prerequisites\">10.1 Prerequisites</h3>\n<ul>\n<li>Python 3.10+</li>\n<li>Bittensor wallet (hotkey + coldkey)</li>\n<li>TAO for registration burn</li>\n<li>For relay miners: funded wallets on all supported destination chains</li>\n</ul>\n<h3 id=\"102-scanner-miner-setup\">10.2 Scanner Miner Setup</h3>\n<p><strong>Step 1: Configure environment variables</strong></p>\n<pre><code class=\"language-bash\"># Chains to monitor (comma-separated chain IDs from the on-chain registry)\nENTANGLE_SCANNER_CHAINS=ethereum_mainnet,solana_mainnet,cosmos_mainnet\n\n# RPC endpoint per monitored chain (private/dedicated strongly recommended)\nENTANGLE_SCANNER_RPC_ETHEREUM_MAINNET=https://your-private-eth-rpc.com\nENTANGLE_SCANNER_RPC_SOLANA_MAINNET=https://your-private-sol-rpc.com\nENTANGLE_SCANNER_RPC_COSMOS_MAINNET=https://your-private-cosmos-rpc.com\n\n# Bittensor credentials\nENTANGLE_BITTENSOR_HOTKEY=your_hotkey\nENTANGLE_BITTENSOR_COLDKEY=your_coldkey\n\n# Role declaration\nENTANGLE_SCANNER_ROLE_DECLARATION=scanner\n</code></pre>\n<p><strong>Step 2: Register on the subnet</strong></p>\n<pre><code class=\"language-bash\">btcli subnet register --netuid &lt;entangle_subnet_id&gt;\n</code></pre>\n<p><strong>Step 3: Start the scanner</strong></p>\n<pre><code class=\"language-bash\">python neurons/miner.py --relay.chains ethereum_mainnet,solana_mainnet\n</code></pre>\n<blockquote>\n<p><strong>RPC Quality is Critical:</strong> A scanner miner competing on validator receipt time with a shared public RPC will systematically lose to operators with private, low-latency infrastructure.</p>\n</blockquote>\n<h3 id=\"103-relay-miner-setup\">10.3 Relay Miner Setup</h3>\n<p><strong>Step 1: Configure environment variables</strong></p>\n<p>For each chain you support:</p>\n<pre><code class=\"language-bash\"># RPC endpoint\nENTANGLE_PROTOCOL_RPC_ETHEREUM=https://eth.llamarpc.com\n\n# Private key (relay execution wallet — must be funded)\nENTANGLE_PROTOCOL_KEY_ETHEREUM=0x&lt;private_key_hex&gt;\n\n# Entangle ProtocolHub contract address\nENTANGLE_PROTOCOL_CONTRACT_ETHEREUM=0x&lt;ProtocolHub_deployed_address&gt;\n</code></pre>\n<p>Repeat for each chain: <code>SOLANA</code>, <code>SUI</code>, <code>STELLAR</code>, <code>COSMOSHUB</code>, <code>ARBITRUM</code>, <code>BASE</code>, <code>POLYGON</code>, <code>OPTIMISM</code>, etc.</p>\n<p><strong>Step 2: Configuration flags</strong></p>\n<pre><code class=\"language-bash\">--relay.max_fee_usd 10           # Maximum USD gas per relay (default $10)\n--relay.max_concurrent 12        # Parallel relay executions (default 12)\n--relay.deadline_buffer_secs 20  # Reject tasks with &lt;20s to deadline\n--relay.chains ethereum,arbitrum # Explicit list of supported chains\n</code></pre>\n<p><strong>Step 3: Start the relay miner</strong></p>\n<pre><code class=\"language-bash\">python neurons/miner.py --relay.chains ethereum,arbitrum,solana\n</code></pre>\n<h3 id=\"104-miner-type-declaration-healthchecksynapse\">10.4 Miner Type Declaration (HealthCheckSynapse)</h3>\n<p>Validators poll miners every ~10 minutes via <code>HealthCheckSynapse</code>. Declare your role in the response:</p>\n<pre><code class=\"language-json\">// Scanner-only\n{ &quot;version&quot;:&quot;1.2.0&quot;, &quot;roles&quot;:[&quot;scanner&quot;], &quot;supported_chains&quot;:[&quot;ethereum_mainnet&quot;,&quot;solana_mainnet&quot;] }\n\n// Relay-only\n{ &quot;version&quot;:&quot;1.2.0&quot;, &quot;roles&quot;:[&quot;relay&quot;], &quot;dst_chains&quot;:[&quot;ethereum_mainnet&quot;,&quot;arbitrum&quot;] }\n\n// Combined\n{ &quot;version&quot;:&quot;1.2.0&quot;, &quot;roles&quot;:[&quot;scanner&quot;,&quot;relay&quot;], &quot;supported_chains&quot;:[&quot;ethereum_mainnet&quot;], &quot;dst_chains&quot;:[&quot;arbitrum&quot;] }\n</code></pre>\n<p>Miners that don&#39;t respond within 60 seconds are excluded from all scoring rounds until the next successful check.</p>\n<h3 id=\"105-private-mev-protection-for-relay-miners\">10.5 Private MEV Protection for Relay Miners</h3>\n<p>When submitting EVM relay transactions, use private mempool routing to prevent MEV front-running:</p>\n<table>\n<thead>\n<tr>\n<th>Chain</th>\n<th>Recommended Private RPC</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Ethereum</td>\n<td>Flashbots Protect (<code>https://rpc.flashbots.net</code>) or MEV Blocker (<code>https://rpc.mevblocker.io</code>)</td>\n</tr>\n<tr>\n<td>Polygon</td>\n<td>Polygon Blocknative private mempool</td>\n</tr>\n<tr>\n<td>Arbitrum / Optimism / Base</td>\n<td>Standard submission (sequencer-ordered; private RPC recommended as best practice)</td>\n</tr>\n</tbody></table>\n<hr>\n"
    },
    {
      slug: "supported-chains",
      title: "11  Supported Chains",
      description: "Supported Chains documentation",
      content: "<h1 id=\"supported-chains\">Supported Chains</h1>\n<h3 id=\"111-launch-chain-support\">11.1 Launch Chain Support</h3>\n<table>\n<thead>\n<tr>\n<th>Chain</th>\n<th>Ecosystem</th>\n<th>Finality Model</th>\n<th>Confirmation Blocks</th>\n<th>Signature Scheme</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Ethereum mainnet</td>\n<td>EVM</td>\n<td>Probabilistic (PoS)</td>\n<td>2 (safe head); 32 for epoch finality</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>Arbitrum One</td>\n<td>EVM L2</td>\n<td>Sequencer + L1 finality</td>\n<td>1 (optimistic)</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>Base</td>\n<td>EVM L2</td>\n<td>Sequencer + L1 finality</td>\n<td>1 (optimistic)</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>Optimism</td>\n<td>EVM L2</td>\n<td>Sequencer + L1 finality</td>\n<td>1 (optimistic)</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>Polygon</td>\n<td>EVM PoA</td>\n<td>Checkpoint</td>\n<td>64</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>BNB Chain</td>\n<td>EVM PoA</td>\n<td>Checkpoint</td>\n<td>64</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>Avalanche</td>\n<td>EVM (Snowman)</td>\n<td>Instant after acceptance</td>\n<td>1</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>Solana mainnet</td>\n<td>Solana</td>\n<td>Tower BFT</td>\n<td>32 slots (~13s)</td>\n<td>ed25519</td>\n</tr>\n<tr>\n<td>SUI mainnet</td>\n<td>SUI</td>\n<td>Narwhal/Bullshark</td>\n<td>1 checkpoint</td>\n<td>ed25519</td>\n</tr>\n<tr>\n<td>Cosmos Hub</td>\n<td>Cosmos/IBC</td>\n<td>Tendermint BFT</td>\n<td>1 block (instant)</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>Osmosis</td>\n<td>Cosmos/IBC</td>\n<td>Tendermint BFT</td>\n<td>1 block (instant)</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>Neutron</td>\n<td>Cosmos/IBC</td>\n<td>Tendermint BFT</td>\n<td>1 block (instant)</td>\n<td>secp256k1</td>\n</tr>\n<tr>\n<td>Stellar mainnet</td>\n<td>Stellar</td>\n<td>SCP (federated)</td>\n<td>1 ledger close</td>\n<td>ed25519</td>\n</tr>\n</tbody></table>\n<p><strong>Phase 2 additions:</strong> SUI (additional), Cosmos Hub, Osmosis, Neutron<br><strong>Phase 3 additions:</strong> Stellar, additional Cosmos chains</p>\n<h3 id=\"112-finality-confirmation-depths-constantspy\">11.2 Finality Confirmation Depths (constants.py)</h3>\n<pre><code class=\"language-python\">FINALITY_BLOCKS: dict[str, int] = {\n    &#39;ethereum&#39;:   12,   # ~2.5 min; PoS single-slot final at 2 epochs\n    &#39;arbitrum&#39;:    1,   &#39;optimism&#39;:  1,   &#39;base&#39;: 1,\n    &#39;polygon&#39;:    64,   &#39;bsc&#39;:      64,\n    &#39;avalanche&#39;:   1,   # Snowman: instant after acceptance\n    &#39;solana&#39;:      1,   # Confirmed commitment level\n    &#39;sui&#39;:         1,   &#39;stellar&#39;: 1,\n    &#39;cosmoshub&#39;:   1,   &#39;osmosis&#39;: 1,   &#39;neutron&#39;: 1,\n}\n</code></pre>\n<h3 id=\"113-chainadapter-interface\">11.3 ChainAdapter Interface</h3>\n<p>All adapters implement the same abstract interface — the only component containing ecosystem-specific code:</p>\n<pre><code class=\"language-python\">class ChainAdapter(ABC):\n    @abstractmethod\n    def get_pending_messages(self, from_height, to_height) -&gt; list[PendingMessage]: ...\n\n    @abstractmethod\n    def is_message_delivered(self, seq_no, src_chain) -&gt; bool: ...\n\n    @abstractmethod\n    def relay_message(self, msg, private_key, max_fee_usd) -&gt; RelayResult: ...\n\n    @abstractmethod\n    def verify_relay(self, relay_tx, expected_seq) -&gt; tuple[bool, raw_proof_dict]: ...\n\n    @abstractmethod\n    def get_rpc_latency_ms(self) -&gt; float: ...\n\n    @abstractmethod\n    def get_wallet_info(self, private_key) -&gt; WalletInfo: ...\n\n    @abstractmethod\n    def get_current_height(self) -&gt; int: ...\n</code></pre>\n<p><strong>Ecosystem-specific implementations:</strong></p>\n<ul>\n<li><strong>EVMAdapter</strong> — handles all 7 EVM chains via web3.py. PoA chains (Polygon, BNB) get <code>ExtraDataToPOAMiddleware</code> injected automatically.</li>\n<li><strong>SolanaAdapter</strong> — uses solana-py and the Anchor IDL. Parses base64-encoded Anchor events using discriminators.</li>\n<li><strong>SUIAdapter</strong> — uses SUI JSON-RPC API via aiohttp. Object-centric execution via programmable transaction blocks.</li>\n<li><strong>StellarAdapter</strong> — split architecture: base ledger via Horizon REST API, smart contracts via Soroban RPC.</li>\n<li><strong>CosmosAdapter</strong> — uses Cosmos REST API (Stargate) and CosmWasm execution. Handles per-chain denom differences.</li>\n</ul>\n<hr>\n"
    },
    {
      slug: "fee-model-gas-oracle",
      title: "12  Fee Model & Gas Oracle",
      description: "Fee Model & Gas Oracle documentation",
      content: "<h1 id=\"fee-model-gas-oracle\">Fee Model & Gas Oracle</h1>\n<h3 id=\"121-fee-architecture\">12.1 Fee Architecture</h3>\n<p>Every cross-chain message triggers a fee payment split at the source chain contract:</p>\n<pre><code>sendMessage(dst_chain, dst_addr, payload) payable:\n  total_fee = platform_base_fee[dst_chain] + gas_reserve[dst_chain]\n  require(msg.value &gt;= total_fee)\n\n  platform_fee     → protocol_treasury_address (e.g. 20% of total_fee)\n  relay_reserve    → relay_reserve_pool[dst_chain]\n  emit MessageDispatched(...)\n</code></pre>\n<h3 id=\"122-gas-oracle\">12.2 Gas Oracle</h3>\n<p>The gas oracle dynamically adjusts <code>gas_reserve[dst_chain]</code> to match actual destination chain gas costs. Validators maintain the oracle via consensus:</p>\n<pre><code>Oracle update cycle (per validator, every ~10 blocks / ~2 minutes):\n  1. Validator samples current gas price on dst_chain via RPC\n  2. Estimates gas cost of a typical receiveEntangleMessage() call\n  3. Submits updateGasOracle(dst_chain, gas_cost_estimate) on-chain\n  4. Contract accepts if caller is a registered validator\n  5. Contract computes stake-weighted median of recent submissions\n  6. Updates gas_reserve[dst_chain] = median × gas_buffer_multiplier\n\n  gas_buffer_multiplier default: 1.25 (25% safety margin above median)\n</code></pre>\n<p><strong>Oracle Collusion Circuit Breaker:</strong> The contract rejects any gas oracle update that deviates more than 30% from the previous epoch&#39;s accepted value, unless countersigned by the protocol multisig.</p>\n<h3 id=\"123-fee-to-quality-flywheel\">12.3 Fee-to-Quality Flywheel</h3>\n<pre><code>High relay volume\n  → More platform fees and relay reserves collected\n  → Larger treasury for protocol sustainability\n  → Relay miners can support more chains and higher volumes\n  → Better miners attracted → lower latency, lower fees\n  → More dApps integrate → higher relay volume\n  → (repeat)\n</code></pre>\n<hr>\n"
    }
    ]
  },
  {
    title: "ECONOMICS & SAFETY",
    pages: [
    {
      slug: "security-model",
      title: "13  Security Model",
      description: "Security Model documentation",
      content: "<h1 id=\"security-model\">Security Model</h1>\n<h3 id=\"131-threshold-attestation\">13.1 Threshold Attestation</h3>\n<p>All message delivery is gated by a <strong>threshold signature bundle</strong> — validators must collectively attest to a message before it can be executed on the destination chain. Compromising the attestation requires controlling &gt;60% of registered validator stake — the same threshold needed to corrupt Bittensor consensus itself.</p>\n<p><strong>EVM (Solidity):</strong></p>\n<pre><code class=\"language-solidity\">function executeMessage(\n    string calldata src_chain,\n    uint64 seq_no,\n    address dst_recipient,\n    bytes calldata payload,\n    AttestationBundle calldata bundle\n) external nonReentrant {\n    // 1. Verify dedup (4-tuple)\n    bytes32 key = _execKey(src_chain, DST_CHAIN_NAME, seq_no);\n    require(!_executed[key], &#39;AlreadyExecuted&#39;);\n\n    // 2. Verify attestation bundle — BEFORE any state change\n    bytes32 payload_hash = keccak256(payload);\n    uint128 attestedStake = 0;\n    for (uint i = 0; i &lt; bundle.validator_uids.length; i++) {\n        ValidatorRecord memory v = validators[bundle.validator_uids[i]];\n        require(v.active, &#39;InactiveValidator&#39;);\n        require(bundle.payload_hashes[i] == payload_hash, &#39;PayloadMismatch&#39;);\n        require(bundle.dst_recipients[i] == dst_recipient, &#39;RecipientMismatch&#39;);\n        // Verify signature from validator&#39;s registered chain key\n        bytes32 digest = _attestDigest(src_chain, seq_no, DST_CHAIN_NAME, payload_hash, dst_recipient);\n        require(_recoverSigner(digest, bundle.signatures[i]) == v.signingKey, &#39;InvalidSig&#39;);\n        attestedStake += v.stakeWeight;\n    }\n    uint128 required = totalRegisteredStake * THRESHOLD_PERCENT / 100;\n    require(attestedStake &gt;= required, &#39;BelowStakeThreshold&#39;);\n\n    // 3. Mark executed and run payload — AFTER threshold verified\n    _executed[key] = true;\n    emit MessageExecuted(seq_no, msg.sender, dst_recipient);\n    (bool ok,) = dst_recipient.call{gas:100_000}(payload);\n}\n</code></pre>\n<h3 id=\"132-security-properties\">13.2 Security Properties</h3>\n<p><strong>Payload integrity:</strong> The contract hashes the submitted payload and requires validators&#39; signed commitments to match. The miner cannot substitute payload without all attesting validators having signed a different hash.</p>\n<p><strong>Recipient integrity:</strong> <code>dst_recipient</code> is explicitly covered in the attestation digest. Substituting it invalidates every validator&#39;s signature simultaneously.</p>\n<p><strong>Pre-execution enforcement:</strong> The stake threshold check is the first gating operation in <code>executeMessage()</code>. Nothing executes until it passes.</p>\n<p><strong>No new trust set:</strong> Bittensor validators are already economically incentivised to attest honestly via TAO staking. A colluding validator loses stake through slashing and reputation.</p>\n<h3 id=\"133-source-chain-reorg-protection\">13.3 Source Chain Reorg Protection</h3>\n<p>Validators do not advance their block cursor to the chain tip immediately. They respect per-chain finality depths before attesting:</p>\n<pre><code class=\"language-python\">current_height = await adapter.get_current_height()\nsafe_height    = current_height - FINALITY_BLOCKS[chain]\n# Validator does NOT attest to events above safe_height\nmsgs = await adapter.get_pending_messages(from_height=self.cursors[chain], to_height=safe_height)\nself.cursors[chain] = safe_height\n</code></pre>\n<h3 id=\"134-cross-validator-collusion-detection\">13.4 Cross-Validator Collusion Detection</h3>\n<p>Validators gossip score vectors to each other via <code>ScoreGossip</code> synapse after each epoch. Because most scores are deterministic from on-chain data, a validator whose submitted weights deviate significantly from the stake-weighted median is producing provably wrong scores:</p>\n<pre><code class=\"language-python\">def _detect_collusion(self, gossip_pool, uid):\n    scores = {g.validator_uid: g.score_vec for g in gossip_pool if g.miner_uid == uid}\n    median_vec = stake_weighted_median(scores, self.metagraph.S)\n    for vuid, vec in scores.items():\n        deviation = mean_absolute_error(vec, median_vec)\n        if deviation &gt; 0.15:   # &gt;15% mean deviation from median\n            self._flag_validator(vuid, deviation)\n            # Flagged validators&#39; weight contributions are discounted\n</code></pre>\n<hr>\n"
    },
    {
      slug: "tokenomics",
      title: "14  Tokenomics",
      description: "Tokenomics documentation",
      content: "<h1 id=\"tokenomics\">Tokenomics</h1>\n<h3 id=\"141-the-alpha-token\">14.1 The Alpha Token</h3>\n<p>Alpha is Entangle&#39;s subnet token on Bittensor. It is a <strong>work token</strong> (utility token) — not a profit-sharing security. Alpha holders benefit from two demand drivers:</p>\n<ol>\n<li><strong>TAO emissions:</strong> Bittensor emits TAO to the subnet, which converts to Alpha via the subnet&#39;s internal AMM</li>\n<li><strong>Fee buyback:</strong> Protocol fee revenue is used to buy Alpha on the open market (programmatic demand backed by real revenue)</li>\n</ol>\n<h3 id=\"142-revenue-buyback-mechanism\">14.2 Revenue Buyback Mechanism</h3>\n<pre><code>Step 1  dApp relays a message — $0.08 fee paid in ETH\nStep 2  Relay confirmed → 70% to winning relay miner ($0.056)\nStep 3  30% → Treasury ($0.024 per message)\nStep 4  20% of treasury → TWAP buyback of Alpha via DEX (every 7 days)\nStep 5  Bought Alpha is burned or locked in governance staking pool\n</code></pre>\n<p><strong>Monthly Buyback Formula:</strong></p>\n<pre><code>MonthlyBuybackUSD = DailyMessages × ProtocolFee × 0.20 × 30\n\nY1 baseline (50K msgs/day, $0.08/msg):\n  MonthlyBuybackUSD = 50,000 × $0.08 × 0.20 × 30 = $24,000/month\n\nY3 scale (3M msgs/day):\n  MonthlyBuybackUSD = 3,000,000 × $0.05 × 0.20 × 30 = $900,000/month\n</code></pre>\n<h3 id=\"143-supply-dynamics-over-time\">14.3 Supply Dynamics Over Time</h3>\n<table>\n<thead>\n<tr>\n<th>Period</th>\n<th>Daily Msgs</th>\n<th>Monthly Emission (α)</th>\n<th>Monthly Buyback (α)</th>\n<th>Burn (20% rate)</th>\n<th>Net Supply Change</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Month 1</td>\n<td>5,000</td>\n<td>937,500</td>\n<td>10,667</td>\n<td>187,500</td>\n<td>+739,333 (High inflation)</td>\n</tr>\n<tr>\n<td>Month 6</td>\n<td>30,000</td>\n<td>937,500</td>\n<td>64,000</td>\n<td>187,500</td>\n<td>+686,000 (Moderate)</td>\n</tr>\n<tr>\n<td>Month 12</td>\n<td>50,000</td>\n<td>937,500</td>\n<td>106,667</td>\n<td>187,500</td>\n<td>+643,333 (Controlled)</td>\n</tr>\n<tr>\n<td>Month 24</td>\n<td>500,000</td>\n<td>937,500</td>\n<td>1,066,667</td>\n<td>187,500</td>\n<td>-316,667 (<strong>Net deflationary</strong>)</td>\n</tr>\n<tr>\n<td>Month 36</td>\n<td>3,000,000</td>\n<td>937,500</td>\n<td>6,000,000</td>\n<td>187,500</td>\n<td>-5,250,000 (Strongly deflationary)</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p><strong>Deflationary Inflection Point:</strong> At ~500,000 messages/day, buyback alone exceeds new Alpha emission. Combined with burn rate, the protocol becomes structurally deflationary.</p>\n</blockquote>\n<h3 id=\"144-emission-burn-rate\">14.4 Emission Burn Rate</h3>\n<p>The protocol can vote to burn a percentage of miner emission before it reaches miners — a deflationary lever:</p>\n<table>\n<thead>\n<tr>\n<th>Burn Rate</th>\n<th>Net Emission/Day</th>\n<th>Burned α/Day</th>\n<th>When to Use</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>0% (Off)</td>\n<td>31,250 α</td>\n<td>0</td>\n<td>Bootstrap phase</td>\n</tr>\n<tr>\n<td>10%</td>\n<td>28,125 α</td>\n<td>3,125</td>\n<td>Early operation</td>\n</tr>\n<tr>\n<td><strong>20% (Recommended)</strong></td>\n<td><strong>25,000 α</strong></td>\n<td><strong>6,250</strong></td>\n<td><strong>Steady state</strong></td>\n</tr>\n<tr>\n<td>35%</td>\n<td>20,313 α</td>\n<td>10,937</td>\n<td>Bear market defence</td>\n</tr>\n<tr>\n<td>50% (Max)</td>\n<td>15,625 α</td>\n<td>15,625</td>\n<td>Emergency defence</td>\n</tr>\n</tbody></table>\n<h3 id=\"145-miner-economics-at-scale\">14.5 Miner Economics at Scale</h3>\n<p><strong>Relay Miner earnings (40 relay miners, 70% emission pool):</strong></p>\n<table>\n<thead>\n<tr>\n<th>Daily Msgs</th>\n<th>Monthly Fee Revenue</th>\n<th>Relay Pool (70%)</th>\n<th>Per Miner/Month</th>\n<th>Infrastructure</th>\n<th>Monthly Profit</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>5,000 (Bear)</td>\n<td>$400</td>\n<td>$280</td>\n<td>$7 / $210</td>\n<td>$400</td>\n<td><strong>-$190 (loss)</strong></td>\n</tr>\n<tr>\n<td>15,000 (Low)</td>\n<td>$1,200</td>\n<td>$840</td>\n<td>$630</td>\n<td>$400</td>\n<td><strong>+$230</strong></td>\n</tr>\n<tr>\n<td>50,000 (Y1 baseline)</td>\n<td>$4,000</td>\n<td>$2,800</td>\n<td>$2,100</td>\n<td>$400</td>\n<td><strong>+$1,700</strong></td>\n</tr>\n<tr>\n<td>500,000 (Y2 growth)</td>\n<td>$30,000</td>\n<td>$21,000</td>\n<td>$5,250</td>\n<td>$400</td>\n<td><strong>+$4,850</strong></td>\n</tr>\n<tr>\n<td>3,000,000 (Y3 scale)</td>\n<td>$150,000</td>\n<td>$105,000</td>\n<td>$26,250</td>\n<td>$400</td>\n<td><strong>+$25,850</strong></td>\n</tr>\n</tbody></table>\n<p><em>Avg fee = $0.08/msg. Top relay miners win more bids (competitive bidding) and earn 2–5× average.</em></p>\n<h3 id=\"146-protocol-self-sustainability\">14.6 Protocol Self-Sustainability</h3>\n<pre><code>Break-even analysis (TAO-independent, fee revenue only):\n\n  Scanner operational cost:  60 miners × $500/month = $30,000/month\n  Validator operational cost: 12 validators × $1,200/month = $14,400/month\n  Total op cost:              $44,400/month\n\n  Treasury receives 30% of fees:\n  Break-even fee revenue:    $44,400 / 0.30 = $148,000/month\n  Break-even messages:       ~62,000 msgs/day\n\n  With TAO emission (normal case): ~15,000 msgs/day\n</code></pre>\n<p>The Y1 baseline target of 50K/day means the protocol approaches fee-only sustainability by Month 3–4.</p>\n<h3 id=\"147-the-dual-flywheel\">14.7 The Dual Flywheel</h3>\n<pre><code>TAO Demand → more TAO staked in subnet → Alpha price rises\nFee Buyback → market buy pressure → Alpha price rises\n\nNetAlphaDemand = TAO_stake_demand + Fee_buyback_demand\nNetAlphaSupply = Emission × (1-B) - Buyback_burns\n\nAlphaEquilibriumP = NetAlphaDemand / NetAlphaSupply\n</code></pre>\n<p>Most Bittensor subnets rely entirely on TAO emission for miner incentives. If TAO price drops, the entire subnet degrades. Entangle breaks this dependency with a second demand source — protocol fee buybacks in stable USD-denominated value. Relay miners are paid in ETH/BNB/MATIC directly, and Alpha buyback continues in real dollar terms even in a TAO bear market.</p>\n<hr>\n"
    },
    {
      slug: "governance",
      title: "15  Governance",
      description: "Governance documentation",
      content: "<h1 id=\"governance\">Governance</h1>\n<h3 id=\"151-protocol-multisig\">15.1 Protocol Multisig</h3>\n<p>All Entangle governance is conducted through a <strong>3-of-5 Gnosis Safe</strong> (EVM) or equivalent Substrate multisig. All signer addresses are published on-chain. All transactions are publicly verifiable.</p>\n<table>\n<thead>\n<tr>\n<th>Parameter</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Signing threshold</td>\n<td>3-of-5</td>\n</tr>\n<tr>\n<td>Signer addition/removal</td>\n<td>Requires 3-of-5 vote; replacement in same transaction</td>\n</tr>\n<tr>\n<td>Transaction log</td>\n<td>All executions publicly visible on-chain</td>\n</tr>\n<tr>\n<td>Key storage</td>\n<td>Hardware wallets; no hot signer keys</td>\n</tr>\n</tbody></table>\n<p><strong>Per-ecosystem equivalents:</strong></p>\n<ul>\n<li><strong>Solana:</strong> Upgrade authority held by a Squads multisig (3-of-5)</li>\n<li><strong>SUI:</strong> Package upgrade cap owned by a 3-of-5 multisig object</li>\n<li><strong>Cosmos:</strong> Contract admin is a CosmWasm multisig (3-of-5 threshold)</li>\n<li><strong>Stellar:</strong> Contract admin is a Stellar multisig (3-of-5 threshold)</li>\n</ul>\n<p>Signer sets are kept in sync across all chains. Any signer rotation must be applied to all chain multisigs within 24 hours.</p>\n<h3 id=\"152-operations-and-timelocks\">15.2 Operations and Timelocks</h3>\n<table>\n<thead>\n<tr>\n<th>Operation</th>\n<th>Timelock</th>\n<th>Trigger</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Update Chain Configuration Registry</td>\n<td>None</td>\n<td>New chain, contract rotation, deprecation</td>\n</tr>\n<tr>\n<td><code>setBaseFee()</code>, <code>setGasBuffer()</code>, <code>setFeeSplit()</code></td>\n<td>None</td>\n<td>Operational fee adjustments</td>\n</tr>\n<tr>\n<td><code>freezeOracle()</code></td>\n<td>None</td>\n<td>Emergency</td>\n</tr>\n<tr>\n<td><code>withdrawRelayReserve()</code></td>\n<td>None</td>\n<td>Operational</td>\n</tr>\n<tr>\n<td>Change emission split (30/70)</td>\n<td><strong>48 hours</strong></td>\n<td>Governance proposal</td>\n</tr>\n<tr>\n<td><code>updateValidatorSet()</code></td>\n<td><strong>48 hours</strong></td>\n<td>Validator set rotation</td>\n</tr>\n<tr>\n<td><code>setSignatureThreshold()</code></td>\n<td><strong>48 hours</strong></td>\n<td>Security parameter change</td>\n</tr>\n<tr>\n<td>Emergency validator key rotation</td>\n<td>None (emergency)</td>\n<td>Key compromise detected</td>\n</tr>\n<tr>\n<td>Freeze subnet registrations</td>\n<td>None</td>\n<td>Emergency only</td>\n</tr>\n</tbody></table>\n<h3 id=\"153-major-protocol-upgrades\">15.3 Major Protocol Upgrades</h3>\n<p>New ecosystem support and architectural changes follow a structured process:</p>\n<ol>\n<li>Public RFC posted to the project GitHub repository</li>\n<li>Minimum 7-day community comment period</li>\n<li>Multisig review and response to substantive comments</li>\n<li>48-hour timelock on execution (where applicable)</li>\n<li>Public announcement of execution transaction hash</li>\n</ol>\n<h3 id=\"154-bootstrapping-governance\">15.4 Bootstrapping Governance</h3>\n<p><strong>Phase 1 (Synthetic events, weeks 1–4 post-launch):</strong><br>A treasury-controlled test dApp emits synthetic <code>MessageDispatched</code> events on a randomised schedule (10–50 events/day/chain). Scanner miners that correctly report receive a flat 0.50 participation score — no speed-race dynamic.</p>\n<p><strong>Phase 2 (Live scoring):</strong><br>Triggered automatically when real <code>MessageDispatched</code> events reach ≥20 per day per chain. Full rank-based scoring (1.00 / 0.70 / 0.50 / 0.20) resumes.</p>\n<p><strong>Emergency Fallback Scanner:</strong><br>If no scanner miner responds to <code>ChainScanSynapse</code> for 3 consecutive cycles (~90 seconds), validators activate a thin RPC poller built into the validator binary. Events are flagged as validator-originated — no scanner miner receives a score. This exists solely to prevent relay pipeline stalling.</p>\n<hr>\n"
    },
    {
      slug: "risk-factors",
      title: "16  Risk Factors",
      description: "Risk Factors documentation",
      content: "<h1 id=\"risk-factors\">Risk Factors</h1>\n<table>\n<thead>\n<tr>\n<th>Risk</th>\n<th>Impact</th>\n<th>Mitigation</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>All scanner miners go offline simultaneously</td>\n<td>Critical — relay pipeline stalls</td>\n<td>Emergency validator fallback scanner activates after ~90s. Economic incentives (zero score = zero emission) strongly disfavour downtime.</td>\n</tr>\n<tr>\n<td>Scanner miners gaming the speed ranking</td>\n<td>Medium — unfair emission distribution</td>\n<td>Validator uses its own receipt timestamp — miners never submit timing. Fabricated event data scores 0.00.</td>\n</tr>\n<tr>\n<td>Validator oracle collusion (majority stake suppresses gas reserve)</td>\n<td>High — relay miners underfunded</td>\n<td>Circuit breaker rejects oracle updates deviating &gt;30% from prior epoch. Multisig can freeze oracle. All submissions publicly auditable.</td>\n</tr>\n<tr>\n<td>Gas price spike on EVM destination chains</td>\n<td>Medium — relay miners temporarily underfunded</td>\n<td>1.25× gas buffer. Oracle updates every ~2 minutes. Multisig can manually set gas reserve via <code>freezeOracle()</code>.</td>\n</tr>\n<tr>\n<td>Scanner miner fabricated event reports</td>\n<td>Medium — false events enter relay pipeline</td>\n<td>Validator spot-verifies ~20% sampled. Fabricated reports score 0.00. Three flags trigger 24h suspension.</td>\n</tr>\n<tr>\n<td>Attestation key compromise</td>\n<td>High — attacker can sign arbitrary messages</td>\n<td>Keys in HSM/encrypted secrets manager. Single compromised key cannot meet threshold alone. Emergency <code>updateValidatorSet()</code> call.</td>\n</tr>\n<tr>\n<td>Smart contract vulnerability</td>\n<td>High — loss of relay reserve or platform fees</td>\n<td>Formal audit before mainnet. Threshold signature limits blast radius. Bug bounty ($500K cap). Circuit breaker: auto-pause above threshold loss.</td>\n</tr>\n<tr>\n<td>TAO price dependency</td>\n<td>High — scanner miner income collapses in bear market</td>\n<td>Emergency burn rate (up to 50%). Insurance Fund subsidises scanner ops during downturns. Relay miners paid in ETH/BNB/MATIC directly.</td>\n</tr>\n<tr>\n<td>Relay miner cartel</td>\n<td>Medium — price-gouging</td>\n<td>Maximum bid cap. Fallback relay pool if no valid bid within 15 blocks. Slashing for detected bid collusion.</td>\n</tr>\n<tr>\n<td>Bittensor protocol changes</td>\n<td>Low — architecture dependency</td>\n<td>Protocol versioning; fallback single-mechanism design available. Architecture follows official Bittensor multi-mechanism specification.</td>\n</tr>\n</tbody></table>\n<hr>\n"
    },
    {
      slug: "roadmap",
      title: "17  Roadmap",
      description: "Roadmap documentation",
      content: "<h1 id=\"roadmap\">Roadmap</h1>\n<h3 id=\"q1-2025-months-13-phase-0-foundation\">Q1 2025 (Months 1–3) — Phase 0: Foundation</h3>\n<ul>\n<li>Register Entangle subnet on Bittensor testnet</li>\n<li>Deploy 12 initial scanner miners (team-operated) and 5 relay miners</li>\n<li>Finalise smart contracts on ETH, Polygon, BNB</li>\n<li>Complete Audit #1</li>\n<li>Whitelist 3 alpha-partner dApps</li>\n<li>Target: 1,000 msgs/day test volume; seed DEX liquidity with 500K Alpha</li>\n</ul>\n<h3 id=\"q2-2025-months-46-phase-1-mainnet-launch\">Q2 2025 (Months 4–6) — Phase 1: Mainnet Launch</h3>\n<ul>\n<li>Go live on Bittensor mainnet</li>\n<li>Open miner registration; target 40 relay miners, 60 scanner miners</li>\n<li>Launch on 6 chains; first 10 dApp integrations</li>\n<li>Target: 10,000–30,000 msgs/day</li>\n<li>Activate k=2.5× fee structure; begin Alpha buybacks</li>\n</ul>\n<h3 id=\"q3q4-2025-months-712-phase-2-scale-amp-revenue\">Q3–Q4 2025 (Months 7–12) — Phase 2: Scale &amp; Revenue</h3>\n<ul>\n<li>Expand to 12 chains; 50+ dApp integrations</li>\n<li>Launch governance (Alpha voting on k, burn rate)</li>\n<li>Target: 50,000 msgs/day; monthly fee revenue $100K+</li>\n<li>Introduce SLA tiers (Standard/Fast/Express)</li>\n<li>Open Relay Miner SDK v2 with auto-bid optimisation</li>\n</ul>\n<h3 id=\"q1q2-2026-months-1318-phase-3-ecosystem-dominance\">Q1–Q2 2026 (Months 13–18) — Phase 3: Ecosystem Dominance</h3>\n<ul>\n<li>Target: 500K msgs/day; 28 chains</li>\n<li>Enterprise integrations (B2B SDK for exchanges, wallets)</li>\n<li>Monthly revenue $900K; Alpha buyback $180K/month</li>\n<li>Launch Relay Miner Insurance Pool (staked Alpha, earns % of slashing rewards)</li>\n<li>Introduce message routing optimisation (cheapest path algorithm)</li>\n</ul>\n<h3 id=\"2027-months-24-phase-4-decentralised-mesh\">2027 (Months 24+) — Phase 4: Decentralised Mesh</h3>\n<ul>\n<li>Target: 3M+ msgs/day; 60+ chains</li>\n<li>Fully decentralised governance; protocol-owned liquidity for Alpha</li>\n<li>Integrate additional non-EVM chains with dedicated scanner miner types</li>\n<li>Explore zkProof-based scanner verification for trustless scanning</li>\n<li>Monthly revenues $4.5M+</li>\n</ul>\n<hr>\n"
    },
    {
      slug: "glossary",
      title: "18  Glossary",
      description: "Glossary documentation",
      content: "<h1 id=\"glossary\">Glossary</h1>\n<table>\n<thead>\n<tr>\n<th>Term</th>\n<th>Definition</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Alpha</strong></td>\n<td>Entangle&#39;s subnet token on Bittensor. A work/utility token.</td>\n</tr>\n<tr>\n<td><strong>Bittensor</strong></td>\n<td>Decentralised AI network providing the incentive and consensus layer for Entangle</td>\n</tr>\n<tr>\n<td><strong>ChainAdapter</strong></td>\n<td>A Python class implementing chain-specific logic for one blockchain ecosystem</td>\n</tr>\n<tr>\n<td><strong>ChainScanSynapse</strong></td>\n<td>The synapse type sent by validators to scanner miners to request event detection</td>\n</tr>\n<tr>\n<td><strong>Discovery Mechanism</strong></td>\n<td>Mechanism 1 — 30% emission, runs scanner miner scoring</td>\n</tr>\n<tr>\n<td><strong>HealthCheckSynapse</strong></td>\n<td>Fast liveness probe sent by validators every ~10 minutes to all miners</td>\n</tr>\n<tr>\n<td><strong>Metagraph</strong></td>\n<td>Bittensor&#39;s on-chain registry of subnet participants and their weights</td>\n</tr>\n<tr>\n<td><strong>MessageDispatched</strong></td>\n<td>The on-chain event emitted by the Entangle contract when a dApp sends a message</td>\n</tr>\n<tr>\n<td><strong>MessageEnvelope</strong></td>\n<td>Chain-agnostic canonical data structure describing a pending cross-chain message</td>\n</tr>\n<tr>\n<td><strong>ProofBundle</strong></td>\n<td>Cryptographic evidence of delivery returned by a relay miner</td>\n</tr>\n<tr>\n<td><strong>Relay Mechanism</strong></td>\n<td>Mechanism 2 — 70% emission, runs relay miner scoring</td>\n</tr>\n<tr>\n<td><strong>RelayTask</strong></td>\n<td>The primary synapse type carrying message context from validator to miner</td>\n</tr>\n<tr>\n<td><strong>Scanner Miner</strong></td>\n<td>Miner role that monitors source chains for MessageDispatched events (Mechanism 1)</td>\n</tr>\n<tr>\n<td><strong>Relay Miner</strong></td>\n<td>Miner role that executes message delivery to destination chains (Mechanism 2)</td>\n</tr>\n<tr>\n<td><strong>Sealed-Bid Auction</strong></td>\n<td>The validator-run process for selecting which relay miner executes each message</td>\n</tr>\n<tr>\n<td><strong>TAO</strong></td>\n<td>Bittensor&#39;s native token; emitted to subnet participants proportional to performance</td>\n</tr>\n<tr>\n<td><strong>Threshold Attestation</strong></td>\n<td>Cryptographic mechanism requiring &gt;60% of registered validator stake to attest before message execution</td>\n</tr>\n<tr>\n<td><strong>Yuma Consensus</strong></td>\n<td>Bittensor&#39;s consensus mechanism aggregating validator weights to produce TAO emission distribution</td>\n</tr>\n</tbody></table>\n<hr>\n<p><em>Entangle Protocol v2.0 — Protocol Documentation</em><br><em>Cross-Chain Messaging Infrastructure on Bittensor</em></p>\n<blockquote>\n<p>This documentation covers the public technical and philosophical content of Entangle Protocol. For miner setup support or dApp integration assistance, visit the official Discord or GitHub repository.</p>\n</blockquote>\n"
    }
    ]
  }
];

export function getPageBySlug(slug: string) {
  for (const section of docsStructure) {
    const found = section.pages.find((page) => page.slug === slug);
    if (found) return found;
  }
}

export function getSectionBySlug(slug: string) {
  return docsStructure.find((section) => section.pages.some((page) => page.slug === slug));
}

export function getAllPages() {
  return docsStructure.flatMap((section) => section.pages);
}

export function getAdjacentPages(slug: string) {
  const pages = getAllPages();
  const index = pages.findIndex((page) => page.slug === slug);
  return {
    prev: index > 0 ? pages[index - 1] : undefined,
    next: index >= 0 && index < pages.length - 1 ? pages[index + 1] : undefined,
  };
}

export function extractHeadings(content: string) {
  const regex = /<(h2|h3)\s+id="([^"]+)">([^<]+)<\/(h2|h3)>/g;
  const headings: { id: string; title: string; level: 2 | 3 }[] = [];
  let match: RegExpExecArray | null;

  while ((match = regex.exec(content)) !== null) {
    const level = match[1] === "h2" ? 2 : 3;
    const id = match[2];
    let title = match[3];
    title = title.replace(/<[^>]+>/g, ""); // strip inner tags if any
    headings.push({ id, title, level });
  }

  return headings;
}
